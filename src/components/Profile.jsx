import React, { userState, useEffect } from "react";
import { Link } from "react-router-dom";

const Profile = ({
  token,
  setToken,
  profile,
  setProfile,
  deletingPosts,
  postings,
  routines,
  setRoutines,
}) => {
  return <div></div>;
};

export default Profile;
